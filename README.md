# Client NLCS

Giao diện React/Vite gồm ba bước: nhập điểm, chọn nhóm ngành và xem tư vấn.

```powershell
npm.cmd install
npm.cmd run dev
```

Sao chép `.env.example` thành `.env.local` nếu backend không chạy tại
`http://127.0.0.1:8000`:

```env
VITE_API_BASE_URL=http://127.0.0.1:8000
```
